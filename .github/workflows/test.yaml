on: pull_request
name: Test
jobs:
  test:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
    - name: Install Go
      uses: actions/setup-go@v2
      with:
        go-version: 1.15.x
    - name: Checkout code
      uses: actions/checkout@v2
    - name: unit
      run: go test ./...
  integration-test:
    runs-on: ubuntu-latest
    steps:
    - name: import gpg key
      run: gpg --import ./test_files/sops_functional_tests_key.asc
    - name: gen docker
      run: go run ./cmd/cogs gen docker basic.cog.toml
    - name: gen sops
      run: go run ./cmd/cogs gen sops basic.cog.toml
    - name: gen kustomize
      run: go run ./cmd/cogs gen kustomize basic.cog.toml
    - name: gen flat_json
      run: go run ./cmd/cogs gen flat_json basic.cog.toml
    - name: gen complex_json
      run: go run ./cmd/cogs gen complex_json basic.cog.toml
